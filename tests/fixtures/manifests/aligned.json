{
  "schema_version": 1,
  "toolbelt_version": "0.1.0",
  "mode": "existing",
  "tools": [
    {
      "id": "mcp-playwright",
      "state": "installed",
      "catalog_version": "1",
      "mcp_name": "playwright",
      "install_scope": "project",
      "executed_steps": [
        {
          "apply_via": "claude_mcp",
          "argv": ["claude", "mcp", "add", "-s", "project", "playwright", "--", "npx", "-y", "@playwright/mcp@latest"],
          "rollback_argv": ["claude", "mcp", "remove", "-s", "project", "playwright"]
        },
        {
          "apply_via": "codex_mcp",
          "argv": ["codex", "mcp", "add", "playwright", "--", "npx", "-y", "@playwright/mcp@latest"],
          "rollback_argv": ["codex", "mcp", "remove", "playwright"]
        }
      ],
      "secrets_required": []
    },
    {
      "id": "plugin-superpowers",
      "state": "installed",
      "catalog_version": "1",
      "install_scope": "user",
      "executed_steps": [
        {
          "apply_via": "claude_plugin",
          "argv": ["claude", "plugin", "install", "superpowers@claude-plugins-official"],
          "rollback_argv": ["claude", "plugin", "uninstall", "superpowers@claude-plugins-official"]
        }
      ],
      "secrets_required": []
    }
  ]
}
