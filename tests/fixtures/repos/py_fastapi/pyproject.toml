[project]
name = "py-fastapi-fixture"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
